[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "drone-eval"
version = "0.1.0"
description = "Drone mission evaluation system"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
